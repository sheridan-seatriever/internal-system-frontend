import React from 'react';
import styles from './EventSidebar.module.css';

const TaskTab = () => {
  return (
    <div className={`${styles.task_tab} ${'container both'}`}>
      <span/>
    </div>
  )
}

export default TaskTab;